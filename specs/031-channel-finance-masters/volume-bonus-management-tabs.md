# 車行台數獎金：規則管理與試算／結算分頁

## 已確認（2026-09-03）

- 採用方案 B，進入頁面預設為「規則管理」，完整顯示全部合作車行獎金規則，不以銷售台數隱藏。
- 「試算／結算」專門核對各期間、各收款車行的台數與金額，保留未銷售規則顯示切換。
- 既有車行專屬規則仍保留，不擅自合併或改成通用規則；未命名規則原以編號顯示，後續依 `volume-bonus-safe-deletion.md` 改為品牌／能源自動命名，原空白值不回填。

## 實作範圍

- 沿用同一路由，以 `tab=rules`、`tab=settlements` 切換；缺少或未知 tab 回到規則管理，舊 `show=all` 連結仍可看全部規則。
- 規則管理不執行逐單試算，卡片顯示名稱、適用車行、車輛條件、全部期間、門檻、啟用開關及編輯入口。
- 已結算共用條件仍鎖定，入口標示「檢視規則／管理期間」。結算與調整結果及取消連結回到試算分頁，編輯儲存回規則管理。
- 不修改模型、migration、財務計算及結算服務；正式頁面驗證以唯讀交易執行，部署前後比對規則、結算、分攤及收支摘要。

## 驗收

1. 未銷售、停用、通用及車行專屬規則均可直接找到及編輯；舊空白名稱不變。
2. 規則管理不呼叫試算；結果分頁保留各期獨立結算、不可重複結算及歷史鎖定。
3. 編輯、結算、已結算提示、調整及取消都回到正確分頁，保留規則定位。
4. Django 自動測試涵蓋預設／未知分頁、舊網址、唯讀頁面、返回流程與金額一致性。
5. 隔離資料庫實測桌機 1440px、手機 390px／320px，確認按鈕可點、文字完整、無水平溢出；說明頁同步。

## 回復

本次無資料遷移；必要時回復本次程式 commit 並重建 web／worker，無需回滾資料庫。

## 本機驗證

- 全站 728 tests 通過，SQLite 跳過 2 項 PostgreSQL 專用測試；前端 19 tests 通過，system check 與 migration 差異檢查通過。
- 隔離資料庫實測未銷售規則編輯與儲存，正確返回管理分頁及原規則；已結算規則條件仍鎖定，試算／調整取消正確返回結果分頁。
- 1440px、390px、320px 實際畫面均無水平溢出；窄手機主要操作按鈕至少 44px，規則／結果資訊分離，瀏覽器無 JavaScript 錯誤。
- 隔離資料原已結算 1500 元維持不變。正式資料部署前建立唯讀摘要，部署後再次比對；發布檢查紀錄保留於 output/bonus-tabs-release-verification.md。

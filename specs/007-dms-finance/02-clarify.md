# 02 — Clarify：財務結算模組待澄清事項

## 已確認

1. **貨幣**：使用 Float(12,0) 與新台幣，不引入多幣別需求。
2. **唯一性**：每筆銷售訂單只允許一個財務結算記錄（建議加 unique constraint）。
3. **架構**：income/expense 各自獨立表格，以 `type` Selection 區分種類。
4. **自動帶入**：僅在建立時帶入已知欄位對應值，其餘為 0 或空。

## 待確認（未來 Sprint）

1. **結算狀態**：是否需要「草稿 → 確認」的狀態流？目前先省略，僅用 note 欄位標記。
2. **多筆收入同類型**：是否允許同一 `type` 有多筆明細？目前設計允許，靠使用者自行管理。
3. **匯出 Excel**：Phase 4 報表是否需要單筆結算的 Excel 匯出？待 Phase 4 實作。
4. **分期補貼息計算**：是否能從分期條件自動計算？目前手動填入。

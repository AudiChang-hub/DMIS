<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- ===== 車行 Kanban 視圖（手機友善卡片式清單）===== -->
  <record id="view_dealer_kanban" model="ir.ui.view">
    <field name="name">dms.dealer.kanban</field>
    <field name="model">dms.dealer</field>
    <field name="arch" type="xml">
      <kanban class="o_kanban_mobile" sample="1">
        <field name="id"/>
        <field name="name"/>
        <field name="code"/>
        <field name="store_type_id"/>
        <field name="owner_name"/>
        <field name="phone_1"/>
        <field name="visit_count"/>
        <field name="active"/>
        <templates>
          <t t-name="kanban-box">
            <div class="oe_kanban_card oe_kanban_global_click"
                 style="min-height:120px; padding:12px;">
              <!-- 車行代碼 + 名稱 -->
              <div class="o_kanban_record_top mb8">
                <div class="o_kanban_record_headings" style="flex:1; overflow:hidden;">
                  <strong class="o_kanban_record_title"
                          style="font-size:16px; display:block; white-space:nowrap; overflow:hidden; text-overflow:ellipsis;">
                    <t t-if="record.code.value">
                      <span class="text-muted" style="font-size:13px;"
                            t-esc="'[' + record.code.value + '] '"/>
                    </t>
                    <t t-esc="record.name.value"/>
                  </strong>
                  <span class="o_kanban_record_subtitle text-muted"
                        style="font-size:13px;">
                    <t t-if="record.store_type_id.value"
                       t-esc="record.store_type_id.value"/>
                  </span>
                </div>
                <!-- 拜訪次數 badge -->
                <div class="o_kanban_record_top_right">
                  <t t-if="record.visit_count.raw_value > 0">
                    <span class="badge badge-pill badge-primary"
                          style="font-size:13px; padding:4px 8px;"
                          t-esc="record.visit_count.value + ' 次拜訪'"/>
                  </t>
                </div>
              </div>
              <!-- 負責人 + 電話 -->
              <div class="o_kanban_record_bottom" style="margin-top:8px;">
                <div class="oe_kanban_bottom_left" style="font-size:14px;">
                  <t t-if="record.owner_name.value">
                    <i class="fa fa-user text-muted mr4" title="負責人"/>
                    <span t-esc="record.owner_name.value"/>
                  </t>
                </div>
                <div class="oe_kanban_bottom_right" style="font-size:14px;">
                  <t t-if="record.phone_1.value">
                    <i class="fa fa-phone text-muted mr4" title="電話"/>
                    <span t-esc="record.phone_1.value"/>
                  </t>
                </div>
              </div>
            </div>
          </t>
        </templates>
      </kanban>
    </field>
  </record>

  <!-- ===== 更新 act_window 加入 kanban 模式 ===== -->
  <record id="dms_core.action_dealer" model="ir.actions.act_window">
    <field name="view_mode">tree,kanban,form</field>
  </record>

</odoo>

# 報表篩選遮擋與多欄表增量驗證（2026/09/08）

## 範圍

- 使用者紅勾的 13 頁以 `12-selected-report-scope.md` 為準；不再建立獨立傭金頁。
- 本增量修正共用報表介面，保留台數統計所需的多欄彙總、車牌、DMIS 歸屬與金額欄位。

## 遮擋根因與修正

- 原 `.report-filter` z-index 50 高於 `.app-header` 的 30，普通捲動就會蓋住全站導覽。
- 報表根容器建立獨立圖層，篩選、編輯與下拉仍可在內容內分層，但不超越導覽列。
- 常用區只放開始日、結束日、月份、車行；其餘收進進階區。
- 有已使用的進階條件時，自動展開與標示數量；隱藏欄位及交叉篩選參數保留。

## 隔離 UI 實測

- 桌面 1440×900：篩選卡片 top 22.7、導覽列 bottom 72.8 的交疊位置，
  `elementFromPoint(750,35)` 仍屬導覽列；截圖確認不遮蓋。
- 手機 390×844：文件寬 375（不超出視窗），篩選單欄 313.6px。
- 展開進階、開啟品牌選單，選 SUZUKI、套用成功；重新顯示「已使用 1 項」，匯出與明細連結保留品牌條件。
- 此次尺寸確認以實際 viewport 為準；先前尺寸工具只影響其他分頁的嘗試不列為通過。
- 僅驗證本機隔離資料、夜間主題與上述路徑；不代表全系統／全主題驗收。臨時尺寸已還原。

## 自動測試

- 報表測試 69 項通過；Node 圖表互動 4 項通過；JS 語法與 migration 檢查通過。
- 多欄表涵蓋雙分類、匯出、交叉篩選、多筆獎金不倍增訂單、缺收支不補零、人工傭金不重算。
- 新完整回歸與正式部署證據待補，不以本機 UI 通過冒充部署完成。

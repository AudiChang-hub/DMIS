# 1.8.0 驗證紀錄

日期：2026-09-15。以下為提交前已執行結果；CI、正式備份與部署仍須在發布階段完成，不能以本文件代替正式發布成功紀錄。

## 自動測試

- Django system check 通過；makemigrations --check --dry-run 無異動。
- 正式版本歷程檢查通過；不修改已發布紀錄。
- 前端 Node 測試 83 項通過，包含能源交集、付款摘要、同頁更換及保留車主資料。
- 選車／接待／帳號相關回歸 34 項通過；最後調整後選車、隱私與資料庫角色邊界 25 項通過。
- 首次全站 1178 項有 2 個舊 UI 斷言失敗及 4 個 Windows cp950 子程序解碼錯誤；已更新為四項篩選與連結選色的斷言，並以 PYTHONUTF8=1 重跑全站。沒有放寬圖片或未上架資料的權限斷言。
- Windows ops 3 項因非 Linux 環境略過；CI 會在 Linux 執行。PostgreSQL 測試由完整 CI 驗證。

## 實際瀏覽器

使用 computer-use 操作同一 Chrome 瀏覽器與獨立本機合成資料（127.0.0.1:18241），未送出正式訂單：

- 1280px 展示管理與公開入口的品牌、車型、型號、能源別依序同列。
- 390px 公開入口依同樣順序排列；文件寬度等於可視寬度，沒有橫向溢出。
- 已選天空藍直接顯示，更換車色預設收合；現金與 24 期卡片可選，頁底付款摘要即時更新，鍵盤可到接續按鈕。
- 390px 詳細頁的深色外觀、選取框與文字可辨識；不需再次選色。
- 分期公司、24 期、每期金額與開辦費帶入接待摘要；車款與付款段落預設收合。
- 合成草稿重新整理後保留車色、分期與備註；按更換方案展開原表單、清除舊期數要求重選，車主／備註不清空。
- 768px 接待表單仍正常雙欄，無橫向溢出。這是瀏覽器 viewport 模擬，不代表實體手機驗收。

## 發布門檻與界線

- 完整 CI（含 PostgreSQL）通過後才建立 v1.8.0 附註標籤，使用既有部署腳本與新的 1.8.0 備份目錄。
- 部署後需核對版本、SHA、備份、workers、公開健康及已登入選車／管理畫面。
- 正式站僅唯讀驗證，不建立／刪除客戶訂單，不變更權限；本次不改資料庫結構或既有財務公式。

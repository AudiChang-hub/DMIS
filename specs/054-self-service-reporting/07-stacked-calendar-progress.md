# 第一頁堆疊圖與日期層級進度

更新：2026-09-08。此文件記錄開發與驗證，不代表原 22 頁已重建完成。

## 原始頁面已核對設定

來源：Google 原報表總車輛銷售頁 `p_oe0r8mk2wd` 編輯模式，只讀核對。

- 第一長條：領牌年／領牌年月下鑽、預設月；細分 SalesSource，Record Count。
  主分類 24、系列 20（其餘系列歸其他），LicenseDate 遞減、SalesSource 次排序遞減。
- 第二長條：年／LicenseDate 日期下鑽，預設日期；細分 MotorType，Record Count。
  主分類 24、系列 10、不歸其他；日期遞減、Record Count 次排序遞減。
- 兩圖皆有「排除空白資料」、交叉篩選與變更排序；縮放實際為關閉，補正舊文件。
- 第二圖未設定日期範圍維度，不能直接假設與第一圖日期控制完全一致。
- 明細實際顯示 1745 筆，含未填領牌日期與未來日期；不得直接宣稱等於 DMIS
  先前登記日期範圍的 1741 筆。差异仍需逐筆可識別鍵及篩選條件核對。
- MotorType 是計算欄位 `calc_l2y60pz9wd`；歷史擷取記錄為牌別／車種分類，
  不是 DMIS VehicleModelFamily。未核對分類公式與來源欄位映射前，不以機種偷換。
- 查閱後關閉欄位浮層，復原／取消復原均為停用，原報表未修改。

## 本次實作

- 白名單堆疊圖、雙維度明細鍵、最多 2000 細分組合的有界查詢。
- 新增年／日／機種／來源類型／車色維度，以及日期分類近到遠排列。
- 年月日讀者切換不改發布內容；圖、點選明細與 CSV 保持一致。
- 未填領牌日期必須明確設定納入，且指定期間後仍排除，不改財務計算。
- 堆疊區塊提示、鍵盤焦點、同頁明細、可展開的文字替代入口。

## 驗證與待辦

- 報表 37 項自動測試、前端 4 項測試通過；全 sales 回歸 822 項、4 項略過通過。
  全套執行後另補的 CSV／日期明細一致性測試已在 37 項報表套件通過，CI 續跑全套。
- 隔離 SQLite、localhost:8024 實測：預覽每月 3 台，甲 1／乙 2，
  鍵盤顯示提示；發布測試版本後點甲區塊，同頁明細僅 1 張。
- UI 實測修正明細原先只接受 tr 的問題；堆疊 span 不再產生空節點錯誤。
- 手機尺寸工具最初未套用到背景測試頁；後改用作用中頁面驗證，確認
  innerWidth=390、innerHeight=844、scrollWidth=375，無橫向溢出。
  實測堆疊區塊 1 台下鑽 1 張、明細卡片、按年切換及年份明細鍵同步。
  尺寸覆寫已重設，原 Google 報表恢復導向；測試未寫正式營運資料。
- 尚待：原 MotorType 公式核對／對應、系列 Top N／其他與次排序、真正交叉篩選、
  原表明細欄位與資料差異、手機驗證、CI、正式部署、第一頁完整驗收。
- 原報表完整重建驗收進度仍為 0／22，不能用共用元件完成數替代頁數。

### c2395c0 共用功能部署紀錄

- CI 34192789366 通過（SQLite／PostgreSQL、正式映像與安全檢查）。
- T470P 先備份再部署；2026-09-08 14:10 完成，正式入口連續 12 次回應正常。
- PostgreSQL 與 Redis 不重啟，服務均保持健康。
- 後續比對分類與系列上限仍是下一批變更，不混稱為此部署已提供。

## 原車種公式後續實際核對

2026-09-08 由資料來源欄位的 fx 入口讀取，更新按鈕停用，未儲存更動：

```text
CASE
 WHEN REGEXP_MATCH(Model, r"^EV0(62|60L|76|70V|76S|76SZV)|Gogoro|Pulse|S2 ABS|S2ABS") THEN "白牌電車"
 WHEN REGEXP_MATCH(Model, r"JEGO|VIVA|EZ1|EZZY|Ur2") THEN "綠牌電車"
 WHEN REGEXP_MATCH(Model, r"BOBE|SHINE|TSV57|M02|M01|JDL-B1") THEN "微型電車"
 WHEN REGEXP_MATCH(Model, r"UQ|UC|UG|UT|UT125XZ") THEN "速克達"
 WHEN REGEXP_MATCH(Model, r"DRZ-4SM|GSX|DS|DS250") THEN "擋車"
 ELSE "其他"
END
```

[Google 官方說明](https://docs.cloud.google.com/data-studio/regexpmatch)確認整段匹配，
因此 UQ125DA、Pulse Ultra 不可擅自當作包含 UQ／Pulse 而歸到非其他類別。
此規則新增為「舊報表車種分類（比對用）」，歷史訂單使用匯入映射型號；
新訂單使用 DMIS 型號，圖上及 CSV 均說明不代表來源已完全對帳。
不修改機種、傭金與獎金，也不代替未來可編輯的新分類方案。

系列前 N 個依目前顯示主分類中的系列合計排名；可選其他合併或略去，
略去時主分類總數保持完整並顯示警告；合併其他可下鑽，包含未指定系列。
後續已補系列依名稱正反排序；原始 SalesSource 的次排序已可設定，仍需整頁實際驗收。

後續驗證：42 項報表測試涵蓋原分類整段匹配（含字尾／大小寫／換行）、
歷史匯入型號、系列上限、未指定系列的其他群組下鑽及 CSV 合併值。
隔離環境發布第 7 版：乙車行 2 台、其他系列 1 台，點其他同頁只列 1 張。
c2395c0 正式權限驗證：admin 管理／明細／匯出 200，普通使用者設計 403，
私人報表與明細匯出 404；正式唯讀核對 1741 訂單／7 群，臨時工作階段已清除。

## 2026-09-08 明細表與來源五分類（efcb57d 已部署）

- 實際讀取原編輯器 Sales Source：空白／中古車／假展場為馭盛；平台為
  `yahoo|百利市|momo|PC|Friday|燦坤|小樹購|蝦皮|YAHOO|Yahoo|Yahoo+假展場` 整段匹配；
  文傑／峻生為店內員工，展場為展場，其餘為車行。
- 原式 `Yahoo+假展場` 的 `+` 是量詞，不能擅自轉成字面加號或包含匹配。
  讀取歷史 `dealer_name_raw`，不以匯入後正規化通路名稱取代；新單使用目前通路。
  SQLite／PostgreSQL JSON null 與文字 `null` 分開處理。
- 堆疊系列可按合計或名稱正反排序；五分類與 MotorType 的堆疊／圓環共用穩定顏色。
- 明細欄位採白名單，可設定每頁 10／25／50 筆與純明細報表；CSV 上限 5000，
  保留固定範圍、讀者篩選及版本檢查，防範公式注入，不輸出原始 JSON／證件／聯絡資訊。
- 實收使用 DMIS 已確認 PaymentRecord 合計；歷史收款價另欄標示來源原值。
  不修改任何正式訂單、付款、傭金或獎勵。
- `create_source_report_draft` 僅建立四圖＋明細核對草稿，要求有效 admin，
  不自動發布，重跑不覆蓋同名草稿，留下 ReportRevision；已更名的草稿需人工確認以避免另建。
- 本機報表測試 53 項通過、JS syntax check 通過、migration check 無變更。
  完整測試 839 項：835 通過、4 略過；前端互動 helper 4 項通過。
  隔離草稿已實際預覽／發布四圖與明細，桌機多欄改為表內橫向捲動，
  手機 390 像素量測整頁 scrollWidth 375，未左右溢出。
  CI 34197591220 通過（含 PostgreSQL、套件與程式安全檢查、正式映像與最小 DB 權限）。
  15:15 部署 efcb57d，Git 與部署標記一致；備份 dmis_20260908_151502.sql.gz 通過 gzip 檢查，
  正式網域連續 12 次 Django 回應，DB／Redis uptime 未改變。
  正式唯讀四張候選圖與明細 1744 筆一致，每群下鑽吻合；舊發布報表 1741／7 群、
  固定條件與 DMIS 財務彙總、admin／一般帳號 HTTP 權限驗證通過，臨時 session 已清除。
  正式僅建立 admin 核對草稿 #2，尚未發布，不修改訂單及帳務。
- 正式唯讀統計：1744 張訂單、全部 completed、3 張無領牌日期；原報表表格 1745 筆。
  後續已核對原表格按 9 維度分組再加總收款價，1745 是分組列數，不能直接認定少一張訂單。
  差異原因未確定，不可藉補造訂單使數字一致。
- 完整驗收仍為 0／22：第一頁跨圖交叉篩選、原 Model 非空白條件與逐筆對帳尚未完成。

## 跨圖選取增量（4f41e9b 已部署）

- 讀者可切換點選行為：原同頁明細，或篩選整頁圖表與明細。
- 不同圖表選取取交集；同一圖再次選取替換，提供單項移除入口；目前非同圖多分類複選。
- 選取只引用已發布的圖表編號、合法下鑽 key 與當次日期層級；最多 8 項，
  不接受任意欄位或 SQL，來源圖固定範圍不會因跨图套用而消失。
- 保存選取當時的日期層級，改年／月／日時不重新解釋舊 key。
- 頁面、明細、圖表 CSV、訂單 CSV 都要求目前發布版本；舊版或缺版號回 409。
- 合併其他系列是動態 Top N 集合，目前保留精確明細，不提供全頁交叉篩選；UI 明示限制。
- 57 項報表測試通過；完整測試 843 項（839 通過、4 略過）。
  真實隔離瀏覽器：選原銷售乙車行後 3 圖各 2 張，固定甲車行圖 0 張；
  再選傭金歸屬甲車行後交集各 1 張，固定甲來源圖仍 0 張；提供逐項移除。
  390 像素手機量測 scrollWidth 375，選取標籤包含實際分類名稱，單項移除後還原完整範圍。
  CI 34198809173 通過；15:32 部署 4f41e9b，Git 與部署標記一致。
  備份 dmis_20260908_153212.sql.gz 通過 gzip 檢查；正式唯讀統計、交集與 HTTP 權限驗證通過。

## 第一頁剩餘屬性查核與原型號空值條件

- 兩張占比圖 `.cd-z85jvt8bxd`／`.cd-9z5whv8bxd`：Sales Source／MotorType、
  Record Count、前 10 類、其他合併開啟、LicenseDate 全部期間、Record Count 遞減；
  交叉篩選關閉、變更排序開啟。五／六種類別均不到 10，不會實際觸發其他合併。
- 已經由 Enter 開啟圖表標頭 → 顯示屬性讀取；原站繪圖空白不代表沒有設定。
- 明細 `.cd-p0mc1zp2wd`：9 個維度＋收款價指標；SortLicenseDate 遞減，
  每頁 10 列、摘要關閉、交叉篩選關閉、連結新分頁開啟。
  已讀取 SortLicenseDate（calc_tcel4cm5wd）：
  `CASE WHEN LicenseDate IS NULL THEN DATE(9999, 12, 31) ELSE LicenseDate END`。
  DMIS 以日期遞減、NULL 優先達成相同先後，不寫入虛構日期。
- 四圖及表格均套用「排除 Model 為空值」，不是空字串。新增受控固定條件
  原型號完整性：JSON null／缺鍵排除，空字串或字面 null 保留；新單使用非 NULL 主檔型號。
  正式 1744 筆歷史映射的型號缺鍵／空字串／JSON null 均為 0。
- 「總車輛銷售」模板的來源占比上限修正為 10，整份固定為原型號有值。
  既有草稿不由命令自動覆寫；正式草稿 #2 的更新仍須版本化保存。
- admin 專用管理頁新增摺疊核對紀錄；不開放一般帳號，不冒充整體完成。

## 單圖查看控制（78ec70e 已部署）

- 每圖可個別排序，日期圖可個別按年／月／日；未指定時沿用整頁設定。
- 圖表明細與 CSV 沿用該圖層級；更新一圖保留其他圖設定、品牌條件及跨圖交集。
- 日期選取固定當下層級，不會被另一圖的日期設定重新解讀；不修改發布設定。
- 59 項報表定向測試通過；隔離 UI 已驗證按年後仍保留乙車行 2 張交集。
- 完整測試 845 項（841 通過、4 略過），migration check 無變更、JS 語法檢查通過。
- 桌機獨立年／日設定保留；390 像素手機切日後仍維持 2 張交集，scrollWidth 375。
  手機排序／日期各用完整一列；選單沿用明暗配色、44px 高度，不使用未美化的原生方框。
- CI 34201218167 通過（含 SQLite、PostgreSQL、安全與正式映像）；15:58 部署。
  Git／部署標記一致；備份 dmis_20260908_155813.sql.gz 通過 gzip 檢查；
  正式網域連續 12 次 Django 回應，DB／Redis 保持 3 日 uptime。

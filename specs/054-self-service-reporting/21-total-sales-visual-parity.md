# 總車輛銷售逐頁視覺修正

2026/09/09，使用者明確要求以原頁對照修正，範圍僅本頁。

## 原站唯讀核對

原頁 `p_oe0r8mk2wd`（含使用者提供的 `/u/0/` 連結）四張嵌入圖仍未渲染，但 UI 設定與容器位置可讀。
桌機為兩列：左側水平堆疊長條，右側占比圖；最下方明細。主要篩選是銷售來源及領牌年月。
第一長條確認水平、非百分比堆疊、資料標籤、總計資訊卡、X 軸標籤與格線、頂端靠左圖例，24 分類。
沒有調整原 Google 報表資料或設定，未以空白畫面作為驗收。

## 本次修正

- 明確新增可選 `sales_overview` 閱讀版型，只為總車輛銷售啟用，其餘頁保持原版型。
- 原逐行卡片改成 SVG 座標堆疊圖，分類同列、共用刻度與格線；保留圖形提示、鍵盤及交叉選取。
- 原來源／年月置於主要篩選，其餘條件保留於進階篩選；說明收合而非刪除。
- 統一精簡四圖間距、標頭與占比圖例，分類數字表仍可展開。
- 財務計算、固定條件、原分類及明細欄位不變；正式套用需保留版本與發布紀錄。

## 尚未等效驗收

## 本頁第二階段互動驗收（方案 B）

- 僅 `sales_overview` 移除複選模式勾選，桌機 Ctrl／⌘ 複選，觸控連點增減。
- 選取立即標示，連續操作合併為最後一次查詢；新操作取消過時回應，失敗回復已成功狀態並可重試。
- 長條與占比圖直接提示分類、數值及占比，點選直接同步四圖及下方明細，不再要求展開數字。
- 同列圖卡等高；可見文字不小於頁籤字級，含 SVG 縮放後標籤；手機圖表內捲動而非整頁溢位。
- 保留無 JavaScript 的資料表降級、鍵盤操作、CSV、排序、日期層級、原訂單入口。
- 額外檢查原頁主要篩選、四圖順序、圖例、明細分頁及資料口徑；未核對的差異不得聲稱等效。

### 額外差異盤點

| 項目 | 原頁觀察 | 本次處理／限制 |
| --- | --- | --- |
| 來源選單 | 可搜尋、全選，取消分類即自動載入 | 本頁複選欄自動合併套用；保留套用按鈕供日期等輸入 |
| 清除語意 | 全選代表不排除 | DMIS 保留「未勾選表示不限」明示，避免改變既有網址語意 |
| 圖表操作 | 圖表標頭提供排序、全螢幕及篩選資訊 | 保留排序／日期層級、放大與選取標籤；直接點圖連動 |
| 明細 | 每頁 10 列，領牌年月等 10 欄 | 保留每頁 10 筆與原訂單入口；DMIS 收款與歷史收款分欄，不混算 |
| 筆數口徑 | 原頁顯示 1–10／1745 | 不以 UI 修改強行湊數；原報表群組列與 DMIS 每訂單一列需另作資料核對 |
| 精確外觀 | 四個嵌入圖在此環境仍空白；切換篩選時曾短暫顯示資料集設定錯誤 | 未修改原報表；配色及圓餅細節仍不能宣稱像素等效 |

### 本機驗證紀錄

- 報表 89 項及前端 10 項通過；快速鍵盤 Ctrl 複選兩分類時立即顯示「已選 2 個分類」。
- 非同步結果為候選圖 255、其他三圖各 2、來源明細 2 筆；浮動明細未自動開啟。
- 1600px 桌機左右圖卡底部相同、無橫向溢位；390px 手機頁寬 375px，圖表內部獨立橫捲。
- 桌機／手機可見 HTML 字級未低於頁籤 14.4px；手機 SVG 標籤縮放後 14.73px。

原圖渲染問題仍需可見截圖核對配色、圓環細節及精確像素；不得將此改版聲稱為原頁所有內容已完全一致。

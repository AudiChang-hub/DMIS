<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- ===== 清單視圖 ===== -->
  <record id="view_dealer_tree" model="ir.ui.view">
    <field name="name">dms.dealer.tree</field>
    <field name="model">dms.dealer</field>
    <field name="arch" type="xml">
      <tree string="車行">
        <!-- 標色圓點：固定顯示，不可隱藏，無欄位標題 -->
        <field name="color_tag" widget="dms_color_dot" string="●" nolabel="0"/>
        <!-- 預設顯示（optional="show"）：9 欄，≤ 15 軟性上限 -->
        <field name="code"          optional="show" width="110px"/>
        <field name="name"          optional="show" width="200px"/>
        <field name="owner_name"    optional="show" width="90px"/>
        <field name="store_manager" optional="show" width="90px"/>
        <field name="phone_1"       optional="show" width="120px"/>
        <field name="mobile"        optional="show" width="120px"/>
        <field name="store_type_id" optional="show" width="80px"/>
        <field name="brand_auth_brand_display" optional="show" width="130px"/>
        <field name="active"        optional="show" readonly="1" width="50px"/>
        <!-- 預設隱藏（optional="hide"）：可在欄位選擇器勾選顯示 -->
        <field name="phone_2"                optional="hide" width="120px"/>
        <field name="mobile_fax"             optional="hide" width="120px"/>
        <field name="email"                  optional="hide" width="150px"/>
        <field name="sym_gas_price_list"     optional="hide" width="70px"/>
        <field name="sym_ev_price_list"      optional="hide" width="70px"/>
        <field name="suzuki_gas_price_list"  optional="hide" width="70px"/>
        <field name="suzuki_ev_price_list"   optional="hide" width="70px"/>
        <field name="sym_dispatch_capacity"  optional="hide" width="80px"/>
        <field name="suzuki_dispatch_capacity" optional="hide" width="80px"/>
        <field name="line_group"             optional="hide" width="70px"/>
        <field name="manager_same_as_owner"  optional="hide" width="70px"/>
        <field name="holiday_gift"           optional="hide" width="70px"/>
        <field name="address"                optional="hide" width="200px"/>
        <field name="note"                   optional="hide" width="200px"/>
      </tree>
    </field>
  </record>

  <!-- ===== 表單視圖（4 分頁）===== -->
  <record id="view_dealer_form" model="ir.ui.view">
    <field name="name">dms.dealer.form</field>
    <field name="model">dms.dealer</field>
    <field name="arch" type="xml">
      <form string="車行">
        <sheet>
          <div class="oe_title">
            <label for="name"/>
            <h1><field name="name" placeholder="店名"/></h1>
          </div>
          <group>
            <group>
              <field name="code" readonly="1"/>
              <field name="store_type_id"/>
              <field name="active"/>
            </group>
            <group>
            </group>
          </group>
          <notebook>
            <page string="基本資料">
              <group string="人員">
                <field name="owner_name"/>
                <field name="manager_same_as_owner"/>
                <field name="store_manager"
                       attrs="{'readonly': [('manager_same_as_owner', '=', True)]}"/>
              </group>
              <group string="LINE 群組大頭貼顏色">
                <field name="color_tag" widget="dms_color_dot" nolabel="0"/>
              </group>
              <group string="地點與備註">
                <field name="address" colspan="2"/>
                <field name="address_map_url" widget="url"
                       string="在 Google Maps 開啟"
                       colspan="2"
                       attrs="{'invisible': [('address_map_url', '=', False)]}"/>
                <field name="note" colspan="2"/>
              </group>
            </page>
            <page string="聯絡資訊">
              <group>
                <field name="phone_1"/>
                <field name="phone_2"/>
                <field name="mobile"/>
                <field name="mobile_fax"/>
                <field name="email"/>
              </group>
            </page>
            <page string="價格表／群組">
              <group string="三陽">
                <field name="sym_gas_price_list"/>
                <field name="sym_ev_price_list"/>
              </group>
              <group string="台鈴">
                <field name="suzuki_gas_price_list"/>
                <field name="suzuki_ev_price_list"/>
              </group>
              <separator string="群組活動"/>
              <group>
                <field name="line_group"/>
                <field name="holiday_gift"/>
              </group>
            </page>
            <page string="排車容量">
              <group>
                <field name="sym_dispatch_capacity"/>
                <field name="suzuki_dispatch_capacity"/>
              </group>
              <div class="alert alert-warning" role="alert">
                <span>排車容量不得為負數，儲存時將自動驗證。</span>
              </div>
            </page>
            <page string="品牌授權">
              <field name="brand_auth_ids">
                <tree string="品牌授權" editable="bottom">
                  <field name="brand_id"/>
                  <field name="auth_type"/>
                </tree>
              </field>
            </page>
          </notebook>
        </sheet>
      </form>
    </field>
  </record>

  <!-- ===== 搜尋視圖 ===== -->
  <record id="view_dealer_search" model="ir.ui.view">
    <field name="name">dms.dealer.search</field>
    <field name="model">dms.dealer</field>
    <field name="arch" type="xml">
      <search>
        <field name="name" string="店名"/>
        <field name="owner_name"/>
        <field name="store_manager"/>
        <field name="phone_1"/>
        <field name="mobile"/>
        <field name="address"/>
        <field name="brand_auth_ids" string="品牌" filter_domain="[('brand_auth_ids.brand_id.name', 'ilike', self)]"/>
        <separator/>
        <filter name="filter_active" string="啟用中" domain="[('active','=',True)]"/>
        <filter name="filter_archived" string="已歸檔" domain="[('active','=',False)]"/>
        <filter name="filter_holiday_gift" string="年節送禮" domain="[('holiday_gift','=',True)]"/>
        <filter name="filter_line_group" string="LINE群組" domain="[('line_group','=',True)]"/>
        <separator/>
        <filter name="filter_sym_gas" string="三陽油車價格表" domain="[('sym_gas_price_list','=',True)]"/>
        <filter name="filter_sym_ev" string="三陽電車價格表" domain="[('sym_ev_price_list','=',True)]"/>
        <filter name="filter_suzuki_gas" string="台鈴油車價格表" domain="[('suzuki_gas_price_list','=',True)]"/>
        <filter name="filter_suzuki_ev" string="台鈴電車價格表" domain="[('suzuki_ev_price_list','=',True)]"/>
        <group expand="0" string="分組">
          <filter name="groupby_store_type" string="車行類型" context="{'group_by': 'store_type_id'}"/>
        </group>
      </search>
    </field>
  </record>

  <!-- ===== 動作 (record 形式) ===== -->
  <record id="action_dealer" model="ir.actions.act_window">
    <field name="name">車行</field>
    <field name="res_model">dms.dealer</field>
    <field name="view_mode">tree,form</field>
    <field name="context">{'form_view_initial_mode': 'edit'}</field>
  </record>

  <!-- ===== 選單 ===== -->
  <menuitem id="menu_dms_root" name="車行管理"/>
  <menuitem id="menu_dealer" name="車行" parent="menu_dms_root"
            action="action_dealer" sequence="10"/>

  <!-- ===== Server Action：車行複製 ===== -->
  <record id="action_dealer_duplicate" model="ir.actions.server">
    <field name="name">複製</field>
    <field name="model_id" ref="model_dms_dealer"/>
    <field name="binding_model_id" ref="model_dms_dealer"/>
    <field name="binding_view_types">list</field>
    <field name="state">code</field>
    <field name="code">
for record in records:
    record.copy()
    </field>
  </record>

</odoo>

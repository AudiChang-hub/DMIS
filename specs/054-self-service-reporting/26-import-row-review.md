# 匯入修正定位與同車輛差異核對

2026/09/10 使用者核准 B：修正並補匯時定位並醒目標示待核對欄位。補充情境為退訂後換買家，以及領牌日期改期。

- 只讀比對同一正規化車輛號碼、同車輛類別的既有訂單。列出上次匯入 Excel、系統目前值、本次暫存值；不以列號或姓名猜測訂單。
- 標示車主、日期等主要欄位差異；依序提示換買家待核對、改期待核對、其他差異、主要欄位相同疑似重複。不得宣稱已完整核對收支。
- 保留無快照、超過 5 筆候選及未知錯誤訊息。未知錯誤不亂猜欄位；仍顯示本列問題摘要。
- 自動聚焦第一個問題欄位，多個問題均保留文字、底色及外框；表單驗證錯誤優先。可由摘要快速定位，手機保留可捲動比較表。
- 不自動取消、改配、退費、變更日期、覆寫、補匯或跳過。不修改匯入判定、財務公式、原始 Excel 或既有訂單。實際訂單異動另走既有業務流程。
- 驗證：改期、換買家、無快照、同值、系統值與舊快照不同、正規化號碼、多候選、未知錯誤、HTML 轉義、表單驗證定位與資料無異動；完整測試、CI、部署及正式站唯讀驗收。

# 06 — 進度報告（2026-04-29 維運與 UI 強化）

> 補上自 `feat/015-dms-product-rebuild` 分支 2026-04-29 一日內的修正紀錄，配合 04-tasks.md 的 Phase 10 使用。

## 1. 背景

當天先處理 OrderProcessor → DMIS 兩筆草稿訂單（SO2026047949 / SO2026047950）資料未匯入的問題，後延伸出價格表 UI 與品牌選單可見性等多項維運修正。所有變更皆以非侵入方式（`_inherit`、CSS、view 替換、資料 patch）達成。

## 2. 變更項目

### 2.1 OrderProcessor 同步強化（dms_sale）

| commit | 內容 |
|---|---|
| cb1086e | `_normalize_data` 支援 result.json 新格式（檔名為 key），並對車行名稱去空白後再 fallback 比對（如「昌 億」→「昌億」） |
| ff129f4 | 新增 `_read_xlsx_fallback`：當 result.json 解析後仍無客戶姓名時，讀取資料夾內含「原始資料」sheet 的 `*.xlsx` 補資料（鍵欄位包含 機種/型號、顏色、車行、姓名、生日、身分證、戶籍、手機、Email、是否有汰舊、是否有分期、備註、配件） |
| ca4b6a7 | `dms.sync.log` 表單新增「重新同步」按鈕（`action_resync`）。流程：刪除原訂單與該筆 log → `dms.order.sync._process_folder_by_name(folder_name)` 重新解析（`skip_mtime=True`）→ 跳轉顯示新 log |
| 58e8b57 | list view 加入批次「重新同步」server action，支援多筆勾選 |

對應 spec：`specs/022-orderprocessor-sync/01-spec.md` 已同步補上「xlsx fallback」與「重新同步（手動重試）」段落。

驗證紀錄：用 odoo shell 重跑 `_SUI 125 (UQ125DA)_汰舊_..._0003` 與陳志豪／昌億資料夾，重新建立 SO2026047953（賴佳郁）、SO2026047954（陳志豪），客戶姓名／電話／地址／生日／汰舊旗標皆完整匯入。

### 2.2 價格表 UI（dms_product）

| commit | 內容 |
|---|---|
| 84c14ca | `pricelist_sticky.css` 加 `tbody td.o_sticky_col:focus-within{z-index:10}`、`.o-autocomplete--dropdown{z-index:1080}`，解決品牌等 m2o 下拉被相鄰 sticky 欄遮擋；同時將 7 筆 `EV0*` 產品品牌統一為「台鈴 Suzuki」（修 6 筆原誤標 Aeon）|
| c82c08e | 價格表 tree 第一欄加入「開啟詳細資料」icon button（`fa-external-link`），呼叫 `dms.product.action_open_form`，target=current 跳到 form 編輯，同時保留 `editable="bottom"` inline 編輯 |
| 8295af2 | 產品頁面 tree 同步加入「開啟詳細資料」按鈕（`dms.product.template.action_open_form`） |
| 5d2810d | 排版總調：表格 `width:auto + min-width:100%`、容器 `overflow-x:auto`，tbody/thead 改 `nowrap`，避免欄寬擠壓造成多行；長欄 `gift_note/fees_note/color` 用 ellipsis；字體 13px |
| 9e01936 | 標題列縮為 12px；`gift_note`、`fees_note` 改回 `pre-line` 完整換行顯示；列高 `auto`、`vertical-align:top` |
| 6a1a46b | 車色欄位也改為完整顯示（`pre-line`，不省略） |

### 2.3 品牌選單（dms_core）

| commit | 內容 |
|---|---|
| e13d2e3 | 修 `addons/dms_core/views/brand_views.xml` tree image 非法 `width="40px"` 屬性（OwlError: `Invalid props for component 'ImageField'`），改 `options="{'size':[40,40]}"` |
| 5c42a5e | `menu_brands` 加 `groups="base.group_system"`，並以 odoo shell 清除 dmis_dev 上殘留的「DMS/車行管理者」綁定，確保只有系統管理員可見「品牌」選單 |

## 3. 驗證指令

```bash
# 1. 升級模組
docker compose exec -T odoo bash -c \
  'odoo -c /etc/odoo/odoo.conf --db_host=db --db_user=odoo --db_password=odoo \
   -d dmis_dev -u dms_sale,dms_product,dms_core --stop-after-init --no-http'

# 2. 重啟並 smoke
docker compose restart odoo
bash scripts/smoke_odoo.sh   # 期望：OK: received 303 from /web/login

# 3. 容器健康檢查
docker compose ps

# 4. UI 驗證重點
#  - 銷售管理 > 同步紀錄：表單頂端有「重新同步」按鈕；list 「動作」選單有批次重新同步
#  - 產品管理 > 價格表：機種欄 sticky；品牌下拉不被擋；長文字欄完整顯示；標題 12px
#  - 車行管理 > 品牌：admin 可見、non-admin 不可見；tree image 不再丟 OwlError
```

## 4. 已知尚未處理

- `addons/dms_visit` 中 brand/motor_type 規則化 wizard 已在較早 commit（4b8f74a / 9c1de83）完成基礎修正，後續若有未命中規則需再擴充。
- `dmis_dev` 上 `ir.attachment(128)` 對應 filestore 檔案已遺失；odoo shell 啟動時的 stack trace 不影響運作，僅顯示在 stderr。需另起任務清理失效附件。

## 5. 對應 spec／文件

- `specs/022-orderprocessor-sync/01-spec.md`：已補 xlsx fallback、重新同步段落
- `specs/015-dms-product-rebuild/04-tasks.md`：已新增 Phase 10
- 本檔（`06-progress-2026-04-29.md`）：本日異動彙總
- `output_report/DMIS_專案進度報告_v5.docx`：以 `scripts/build_progress_report.py` 重新產出，新增獨立章節「3. 產品管理（DMS Product）」（產品頁面、車型價格表、價目版本與複製、分期規則模板/費用類型/規則掛接），章節重編號為 4 車銷管理 / 5 銷售分析 / 6 佣金管理 / 7 零件管理 / 8 使用者管理 / 9 驗證；補拍截圖 `24_sync_log_form` / `26_installment_rule` / `27_fee_type` / `28_rule_binding`。
